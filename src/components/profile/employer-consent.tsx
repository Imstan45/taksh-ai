"use client";import{useState}from"react";export function EmployerConsent({initial}:{initial:boolean}){const[consent,setConsent]=useState(initial),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function update(next:boolean){setBusy(true);setMessage("");const response=await fetch("/api/readiness/consent",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({consent:next})}),body=await response.json();if(response.ok){setConsent(next);setMessage("Employer sharing preference saved.")}else setMessage(body.error??"Preference could not be saved.");setBusy(false)}return <section className="mx-auto mt-6 max-w-4xl rounded-2xl border border-zinc-200 bg-white p-6 text-zinc-950"><h2 className="text-xl font-semibold">Employer profile sharing</h2><p className="mt-2 text-sm leading-6 text-zinc-600">Allow Taksh to share your completed candidate profile and resume with relevant employers. This is optional and does not affect your assessment score.</p><label className="mt-5 flex items-start gap-3"><input className="mt-1 size-4" type="checkbox" checked={consent} disabled={busy} onChange={event=>void update(event.target.checked)}/><span className="text-sm">Allow Taksh to share my profile with relevant employers.</span></label>{message?<p className="mt-3 text-sm text-violet-700" role="status">{message}</p>:null}</section>}
