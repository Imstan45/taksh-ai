"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function ManualUpiForm({reference,upiId,upiUri}:{reference:string;upiId:string;upiUri:string}){const[utr,setUtr]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),router=useRouter();async function copy(){await navigator.clipboard.writeText(upiId);setMessage("UPI ID copied.")}async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setMessage("");const response=await fetch("/api/payments/manual-upi/submit",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({reference,utr})}),result=await response.json();setBusy(false);if(!response.ok){setMessage(result.error||"Could not submit payment details.");return}setMessage(result.duplicateFlag?"Submitted for review. This reference also appears on another order, so an administrator will inspect it carefully.":"Payment details submitted for verification.");router.refresh()}return <div><div className="flex flex-wrap gap-2"><button className="rounded-lg border border-zinc-300 px-4 py-2 text-sm font-semibold" type="button" onClick={copy}>Copy UPI ID</button><a className="rounded-lg border border-violet-300 px-4 py-2 text-sm font-semibold text-violet-800 sm:hidden" href={upiUri}>Open UPI App</a></div><form className="mt-7" onSubmit={submit}><label className="text-sm font-semibold" htmlFor="utr">UPI Transaction / UTR Number</label><p className="mt-1 text-xs text-zinc-500">Enter the transaction reference shown in your UPI payment confirmation.</p><input className="field mt-3 border-zinc-300 bg-white text-zinc-950" id="utr" value={utr} onChange={event=>setUtr(event.target.value)} minLength={6} maxLength={40} required autoComplete="off"/><button className="mt-4 w-full rounded-lg bg-violet-700 px-5 py-3 font-semibold text-white disabled:opacity-60" disabled={busy}>{busy?"Submitting securely…":"Submit Payment for Verification"}</button></form>{message?<p className="mt-4 text-sm text-zinc-700" role="status" aria-live="polite">{message}</p>:null}</div>}
