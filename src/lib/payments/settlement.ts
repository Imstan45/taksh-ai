import{prisma}from"@/lib/prisma";import{grantProductEntitlement}from"@/lib/commerce/grant-entitlement";
export async function activatePayment(provider:string,orderId:string,paymentId:string){return prisma.$transaction(async tx=>{const order=(await tx.$queryRaw<Array<{id:string;user_id:string;product_id:string;amount_in_paise:number;currency:string}>>`select id,user_id,product_id,amount_in_paise,currency from public.payment_orders where provider=${provider} and provider_order_id=${orderId} and product_id is not null for update`)[0];if(!order)throw new Error("Order not found");const existing=(await tx.$queryRaw<Array<{id:string}>>`select id from public.payments where provider=${provider} and provider_payment_id=${paymentId}`)[0];if(existing)return{paymentId:existing.id,duplicate:true};const payment=(await tx.$queryRaw<Array<{id:string}>>`insert into public.payments(user_id,payment_order_id,provider,provider_payment_id,provider_order_id,razorpay_payment_id,razorpay_order_id,amount_in_paise,currency,status,verified_at) values(${order.user_id}::uuid,${order.id}::uuid,${provider},${paymentId},${orderId},${provider==="razorpay"?paymentId:null},${provider==="razorpay"?orderId:null},${order.amount_in_paise},${order.currency},'captured',now()) returning id`)[0];if(!payment)throw new Error("Payment settlement failed");await tx.$executeRaw`update public.payment_orders set status='paid',updated_at=now() where id=${order.id}::uuid`;await grantProductEntitlement(tx,{userId:order.user_id,productId:order.product_id,paymentId:payment.id,source:"payment"});await tx.$executeRaw`insert into public.product_events(user_id,event_name,product_id,properties) values(${order.user_id}::uuid,'payment_success',${order.product_id}::uuid,${JSON.stringify({provider,orderId,paymentId,amountInPaise:order.amount_in_paise})}::jsonb)`;await tx.$executeRaw`update public.campaign_attributions set purchased_at=now(),purchased_product_id=${order.product_id}::uuid,revenue_in_paise=${order.amount_in_paise} where id=(select id from public.campaign_attributions where user_id=${order.user_id}::uuid order by first_touched_at desc limit 1)`;return{paymentId:payment.id,duplicate:false}})}
