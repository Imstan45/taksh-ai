import{createHash}from"node:crypto";import{cookies}from"next/headers";import type{Prisma}from"@/generated/prisma/client";import{prisma}from"@/lib/prisma";
export const SALES_REFERRAL_COOKIE="taksh_sales_ref";
export function referralTokenHash(token:string){return createHash("sha256").update(token).digest("hex")}
export async function currentSalesAttribution(userId?:string){if(userId){const linked=(await prisma.$queryRaw<Array<{id:string}>>`select attribution.id from public.sales_referral_attributions attribution join public.sales_challenges challenge on challenge.id=attribution.challenge_id where attribution.registered_user_id=${userId}::uuid and attribution.validity_status='valid' order by attribution.first_visit_at limit 1`)[0];if(linked)return linked.id}const token=(await cookies()).get(SALES_REFERRAL_COOKIE)?.value;if(!token)return null;return(await prisma.$queryRaw<Array<{id:string}>>`select id from public.sales_referral_attributions where visitor_token_hash=${referralTokenHash(token)} and validity_status<>'invalid'`)[0]?.id??null}
export async function attachSalesAttribution(tx:Prisma.TransactionClient,userId:string){const token=(await cookies()).get(SALES_REFERRAL_COOKIE)?.value;if(!token)return null;const row=(await tx.$queryRaw<Array<{id:string;participant_user_id:string;registered_user_id:string|null}>>`select attribution.id,participant.user_id participant_user_id,attribution.registered_user_id from public.sales_referral_attributions attribution join public.sales_challenge_participants participant on participant.id=attribution.participant_id join public.sales_challenges challenge on challenge.id=attribution.challenge_id where attribution.visitor_token_hash=${referralTokenHash(token)} and attribution.validity_status<>'invalid' and challenge.status in('registration_open','active') for update`)[0];if(!row)return null;if(row.participant_user_id===userId){await tx.$executeRaw`update public.sales_referral_attributions set fraud_flag=true,fraud_reason='Participant self-referral',validity_status='review',updated_at=now() where id=${row.id}::uuid`;return null}if(row.registered_user_id&&row.registered_user_id!==userId)return null;const existing=(await tx.$queryRaw<Array<{id:string}>>`select id from public.sales_referral_attributions where registered_user_id=${userId}::uuid and id<>${row.id}::uuid`)[0];if(existing)return null;await tx.$executeRaw`update public.sales_referral_attributions set registered_user_id=${userId}::uuid,registered_at=coalesce(registered_at,now()),is_qualified_registration=true,updated_at=now() where id=${row.id}::uuid`;return row.id}
export async function markSalesAttribution(userId:string,event:"assessment_started"|"assessment_completed"){const column=event==="assessment_started"?"assessment_started_at":"assessment_completed_at";if(column==="assessment_started_at")await prisma.$executeRaw`update public.sales_referral_attributions set assessment_started_at=coalesce(assessment_started_at,now()),updated_at=now() where registered_user_id=${userId}::uuid and validity_status='valid'`;else await prisma.$executeRaw`update public.sales_referral_attributions set assessment_completed_at=coalesce(assessment_completed_at,now()),updated_at=now() where registered_user_id=${userId}::uuid and validity_status='valid'`}
