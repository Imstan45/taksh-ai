export type SalesMetrics={uniqueVisitors:number;qualifiedRegistrations:number;assessmentCompletions:number;payingCustomers:number;grossRevenue:number;refundedRevenue:number;netRevenue:number;complianceScore:number};
export type SalesScore=SalesMetrics&{conversionRate:number;revenueScore:number;customerScore:number;conversionScore:number;conversionProvisional:boolean;finalScore:number;rank:number;sourceIndex:number};
export function scoreChallenge(rows:SalesMetrics[],minimumRegistrations=20):SalesScore[]{const highestRevenue=Math.max(0,...rows.map(row=>row.netRevenue)),highestCustomers=Math.max(0,...rows.map(row=>row.payingCustomers));const conversionRates=rows.map(row=>row.qualifiedRegistrations?row.payingCustomers/row.qualifiedRegistrations:0),eligibleRates=conversionRates.map((rate,index)=>rows[index].qualifiedRegistrations>=minimumRegistrations?rate:rate*Math.min(1,rows[index].qualifiedRegistrations/minimumRegistrations)),highestConversion=Math.max(0,...eligibleRates);const scored=rows.map((row,index)=>{const conversionRate=conversionRates[index],revenueScore=highestRevenue?row.netRevenue/highestRevenue*70:0,customerScore=highestCustomers?row.payingCustomers/highestCustomers*15:0,conversionScore=highestConversion?eligibleRates[index]/highestConversion*10:0,finalScore=revenueScore+customerScore+conversionScore+row.complianceScore;return{...row,sourceIndex:index,conversionRate,revenueScore,customerScore,conversionScore,conversionProvisional:row.qualifiedRegistrations<minimumRegistrations,finalScore,rank:0}}).sort((a,b)=>b.finalScore-a.finalScore||b.netRevenue-a.netRevenue);return scored.map((row,index)=>({...row,rank:index+1}))}
