import { requireCollegeAdmin } from "@/lib/admin-scope";import{parseCsv,studentCsvHeaders}from"@/lib/csv";import{prisma}from"@/lib/prisma";import{createSupabaseAdminClient}from"@/lib/supabase/server";import{createToken,hashToken}from"@/lib/security/tokens";import{mainEnvironment}from"@/lib/env";
type Result={row:number;email:string;ok:boolean;reason?:string};
export async function POST(request:Request){const{session,institutionId}=await requireCollegeAdmin();const data=await request.formData();const file=data.get("file");if(!(file instanceof File)||file.size>2_000_000)return Response.json({error:"A CSV file under 2 MB is required."},{status:400});const rows=parseCsv(await file.text());const headers=(rows.shift()??[]).map(x=>x.toLowerCase());if(studentCsvHeaders.some(header=>!headers.includes(header)))return Response.json({error:`Required columns: ${studentCsvHeaders.join(", ")}`},{status:400});const seen=new Set<string>();const results:Result[]=[];
for(const[index,row]of rows.entries()){const get=(name:string)=>row[headers.indexOf(name)]?.trim()??"";const first=get("first name"),last=get("last name"),email=get("email").toLowerCase(),department=get("department"),batch=get("batch"),roll=get("roll number");const number=index+2;try{if(!first||!last||!email.includes("@")||!department||!batch||!roll)throw new Error("All six fields are required.");if(seen.has(email))throw new Error("Duplicate email in this file.");seen.add(email);const scope=await prisma.$queryRaw<Array<{department_id:string;batch_id:string}>>`SELECT department.id department_id,batch.id batch_id FROM public.departments department JOIN public.academic_batches batch ON batch.department_id=department.id AND batch.institution_id=department.institution_id WHERE department.institution_id=${institutionId}::uuid AND lower(department.code)=lower(${department}) AND lower(batch.name)=lower(${batch}) AND department.status='active' AND batch.status='active'`;if(!scope[0])throw new Error("Department code or batch was not found.");const duplicate=await prisma.$queryRaw<Array<{id:string}>>`SELECT invitation.id FROM public.invitations invitation WHERE lower(invitation.email)=${email} AND invitation.status='pending' AND invitation.expires_at>now() UNION ALL SELECT role.user_id FROM public.user_roles role JOIN auth.users account ON account.id=role.user_id WHERE lower(account.email)=${email} LIMIT 1`;if(duplicate[0])throw new Error("User or pending invitation already exists.");const invitationId=crypto.randomUUID(),token=createToken(),redirectTo=`${mainEnvironment().NEXT_PUBLIC_APP_URL}/accept-invitation?invitation=${invitationId}&invitation_token=${encodeURIComponent(token)}`;const{data:invited,error}=await createSupabaseAdminClient().auth.admin.inviteUserByEmail(email,{redirectTo,data:{full_name:`${first} ${last}`,invitation_id:invitationId}});if(error||!invited.user)throw new Error(error?.message??"Invitation email failed.");await prisma.$transaction(async tx=>{await tx.$executeRaw`INSERT INTO public.invitations(id,email,role,institution_id,department_id,batch_id,token_hash,invited_by,expires_at) VALUES(${invitationId}::uuid,${email},'STUDENT',${institutionId}::uuid,${scope[0].department_id}::uuid,${scope[0].batch_id}::uuid,${hashToken(token)},${session.user.id}::uuid,now()+interval '7 days')`;await tx.$executeRaw`INSERT INTO public.user_roles(user_id,role,institution_id,account_status) VALUES(${invited.user.id}::uuid,'STUDENT',${institutionId}::uuid,'invited')`;await tx.$executeRaw`INSERT INTO public.user_academic_memberships(user_id,institution_id,department_id,batch_id,membership_type,roll_number,active) VALUES(${invited.user.id}::uuid,${institutionId}::uuid,${scope[0].department_id}::uuid,${scope[0].batch_id}::uuid,'STUDENT',${roll},true)`});results.push({row:number,email,ok:true});}catch(error){results.push({row:number,email,ok:false,reason:error instanceof Error?error.message:"Import failed."})}}
return Response.json({total:results.length,successful:results.filter(x=>x.ok).length,failed:results.filter(x=>!x.ok).length,results});}
