import{redirect}from"next/navigation";import{auth}from"@/auth";import{SkillDiagnostic}from"@/components/diagnostic/skill-diagnostic";export const metadata={title:"10-Minute Skill Diagnostic"};export default async function Page(){const s=await auth();if(!s?.user)redirect("/login?callbackUrl=/diagnostic");if(s.user.role!=="STUDENT")redirect("/dashboard");return <main className="min-h-screen bg-[#08090e] px-4 py-8 text-white sm:px-6"><div className="mx-auto max-w-6xl"><SkillDiagnostic/></div></main>}
