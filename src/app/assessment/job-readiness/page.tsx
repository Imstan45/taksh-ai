import Link from"next/link";import{auth}from"@/auth";import{prisma}from"@/lib/prisma";
export const metadata={title:"Placement Readiness Diagnostic | Taksh AI"};
export default async function CampaignDiagnostic({searchParams}:{searchParams:Promise<{source?:string;medium?:string;campaign?:string}>}){const query=await searchParams;const rows=query.campaign?await prisma.$queryRaw<Array<{id:string;name:string;code:string;source:string;assessment_code:string}>>`select id,name,code,source,assessment_code from public.campaigns where code=${query.campaign} and active and (starts_at is null or starts_at<=now()) and (ends_at is null or ends_at>now()) limit 1`:[];const campaign=rows[0];const session=await auth();
 if(!campaign)return <main className="grid min-h-screen place-items-center bg-[#f7f7f8] px-5 text-zinc-950"><section className="max-w-lg rounded-2xl border border-zinc-200 bg-white p-8 text-center"><h1 className="text-2xl font-semibold">This assessment link is unavailable</h1><p className="mt-3 leading-7 text-zinc-600">The campaign may have ended or the link may be incomplete. Ask the sender for a current assessment link.</p><Link className="mt-6 inline-flex text-violet-700" href="/programs">Browse Taksh programs</Link></section></main>;
 const params=new URLSearchParams({source:query.source??campaign.source,medium:query.medium??"campaign",campaign:campaign.code,campaign_id:campaign.id,landing_page:"/assessment/job-readiness"});const diagnostic=`/diagnostic?${params}`;const destination=session?diagnostic:`/signup?next=${encodeURIComponent(diagnostic)}&${params}`;
 return <main className="min-h-screen bg-[#f7f7f8] px-5 py-12 text-zinc-950"><section className="mx-auto max-w-2xl rounded-2xl border border-zinc-200 bg-white p-7 sm:p-10"><p className="text-sm font-semibold text-violet-700">{campaign.name}</p><h1 className="mt-4 text-4xl font-semibold tracking-tight">45-minute Placement Readiness Assessment</h1><p className="mt-5 leading-7 text-zinc-600">Measure quantitative aptitude, logical reasoning, English and your selected technical track across 40 questions. You receive a readiness score and skill breakdown at no cost.</p><ul className="mt-7 space-y-3 text-sm text-zinc-700"><li>40 questions with a visible 45-minute timer</li><li>Choose Python, Java, ServiceNow or General IT</li><li>Your answers save automatically</li><li>Fullscreen secure mode with automatic submission if the tab is switched</li><li>Purchasing never changes your score or hiring eligibility</li></ul><Link className="mt-8 inline-flex rounded-lg bg-violet-700 px-6 py-3 font-semibold text-white" href={destination}>{session?"Continue to instructions":"Create account and continue"}</Link></section></main>}
