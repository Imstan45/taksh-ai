import{auth}from"@/auth";import{prisma}from"@/lib/prisma";import{redirect}from"next/navigation";import{ActionFeedbackForm}from"@/components/feedback/action-feedback-form";import{joinSalesChallenge}from"../actions";
export default async function Join(){const session=await auth();if(!session?.user)redirect("/login?callbackUrl=/sales-challenge/join");const challenge=(await prisma.$queryRaw<Array<{id:string;name:string}>>`select id,name from public.sales_challenges where status in('registration_open','active') and now()<end_at order by start_at limit 1`)[0];if(!challenge)return <main className="mx-auto max-w-2xl px-6 py-16"><h1 className="text-3xl font-semibold">Registration is closed</h1></main>;return <main className="mx-auto max-w-2xl px-6 py-16"><h1 className="text-4xl font-semibold">Join {challenge.name}</h1><ActionFeedbackForm action={joinSalesChallenge} successMessage="Registration complete." pendingMessage="Creating your referral link…" className="glass-card mt-8 space-y-5"><input type="hidden" name="challengeId" value={challenge.id}/><label className="block text-sm">Public display name<input className="field mt-2" name="displayName" required defaultValue={session.user.name??""}/></label><label className="flex gap-3 text-sm text-zinc-300"><input type="checkbox" name="acceptRules" required/>I have read and accept the challenge rules and fair-use policy.</label><button className="btn-primary w-full">Join and create my link</button></ActionFeedbackForm></main>}
