import{afterEach,describe,expect,it}from"vitest";import{createTakshOrderReference,manualUpiConfig,normalizeUtr,upiPaymentUri}from"@/lib/payments/manual-upi";
describe("manual UPI payments",()=>{afterEach(()=>{delete process.env.MANUAL_UPI_ENABLED;delete process.env.TAKSH_UPI_ID});it("creates non-incremental customer order references",()=>{const first=createTakshOrderReference(),second=createTakshOrderReference();expect(first).toMatch(/^TAKSH-[A-F0-9]{6}$/);expect(second).not.toBe(first)});it("normalizes references for duplicate comparisons",()=>{expect(normalizeUtr(" 1234 ab-cd ")).toBe("1234AB-CD")});it("builds QR and deep-link data from the server-only UPI configuration",()=>{const uri=upiPaymentUri({upiId:"taksh@upi",recipientName:"Taksh",amountInPaise:99900,reference:"TAKSH-ABC123"});expect(uri).toContain("pa=taksh%40upi");expect(uri).toContain("pn=Taksh");expect(uri).toContain("am=999.00");expect(uri).toContain("tn=Taksh+TAKSH-ABC123")});it("reads the receiving ID only from TAKSH_UPI_ID",()=>{process.env.TAKSH_UPI_ID=" taksh@upi ";expect(manualUpiConfig()).toMatchObject({enabled:true,upiId:"taksh@upi",recipientName:"Taksh"});process.env.MANUAL_UPI_ENABLED="false";expect(manualUpiConfig().enabled).toBe(false)})});
