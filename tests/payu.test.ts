import{afterEach,describe,expect,it}from"vitest";import{createHash}from"node:crypto";import{createPayuRequest,validPayuResponse}from"../src/lib/payments/payu";
describe("PayU hashing",()=>{afterEach(()=>{delete process.env.PAYU_MERCHANT_KEY;delete process.env.PAYU_MERCHANT_SALT});it("creates the documented SHA-512 hosted checkout hash",()=>{process.env.PAYU_MERCHANT_KEY="merchant";process.env.PAYU_MERCHANT_SALT="salt";const result=createPayuRequest({txnid:"txn1",amount:"499.00",productinfo:"Python Full Stack",firstname:"Surya",email:"learner@example.com",surl:"https://takshai.in/callback",furl:"https://takshai.in/callback"}),expected=createHash("sha512").update("merchant|txn1|499.00|Python Full Stack|Surya|learner@example.com|||||||||||salt").digest("hex");expect(result.fields.hash).toBe(expected)});it("accepts an authentic reverse hash and rejects tampering",()=>{process.env.PAYU_MERCHANT_KEY="merchant";process.env.PAYU_MERCHANT_SALT="salt";const fields={key:"merchant",txnid:"txn1",amount:"499.00",productinfo:"Python Full Stack",firstname:"Surya",email:"learner@example.com",status:"success",udf1:"",udf2:"",udf3:"",udf4:"",udf5:"",hash:""};fields.hash=createHash("sha512").update("salt|success|||||||||||learner@example.com|Surya|Python Full Stack|499.00|txn1|merchant").digest("hex");expect(validPayuResponse(fields)).toBe(true);expect(validPayuResponse({...fields,amount:"1.00"})).toBe(false)})});
