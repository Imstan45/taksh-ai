import{describe,expect,it}from"vitest";import{parseCsv,studentCsvHeaders}from"@/lib/csv";
describe("Student CSV",()=>{it("parses quoted commas and independent rows",()=>{expect(parseCsv('name,email,student_id,department,academic_year,semester,section\n"Ada, A",ada@example.edu,C1,CSE,2026-2027,Semester 1,A')).toHaveLength(2);expect(parseCsv('"Ada, A",ada@example.edu')[0][0]).toBe("Ada, A")});it("defines the required canonical headers",()=>expect(studentCsvHeaders).toEqual(["name","email","student_id","department","academic_year","semester","section"]))});
