import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";
describe("college learning workflow",()=>{const migration=readFileSync("supabase/migrations/202608130001_college_learning_workflow.sql","utf8");it("uses one activity architecture for all three activity types",()=>{expect(migration).toContain("public.learning_activities");expect(migration).toContain("'homework','classwork','assignment'");expect(migration).not.toContain("create table if not exists public.homework")});it("protects submissions and notifications with RLS",()=>{for(const value of["alter table public.activity_submissions enable row level security","activity_submissions_scoped_read","notifications_own_read","student_id=(select auth.uid())"])expect(migration).toContain(value)});it("keeps future AI grading fields without implementing AI grading",()=>{expect(migration).toContain("ai_suggested_score");expect(migration).toContain("ai_feedback");expect(migration).toContain("ai_evaluation")});it("makes aptitude tickets single use",()=>{const questions=readFileSync("src/app/api/assessment/questions/route.ts","utf8"),submit=readFileSync("src/app/api/assessment/submit/route.ts","utf8");expect(questions).toContain("aptitude_assessment_sessions");expect(submit).toContain("submitted_at IS NULL");expect(submit).toContain("FOR UPDATE")});it("derives learning identity from the canonical asset",()=>{const progress=readFileSync("src/app/api/learning/progress/route.ts","utf8");expect(progress).toContain("SELECT a.course,a.module,a.topic,a.subtopic,a.content_version");expect(progress).toContain("${asset.course}");expect(progress).not.toContain("${input.course}")})});
