import { describe,expect,it } from "vitest";import{readFileSync}from"node:fs";
describe("institutional assessment security",()=>{it("does not deliver answer keys before submission",()=>{const source=readFileSync("src/app/api/institutional-assessments/[assessmentId]/start/route.ts","utf8");const response=source.slice(source.indexOf("return {attemptId"));expect(response).not.toContain("correct_answer");expect(response).not.toContain("explanation")});it("locks attempts before scoring to reject replay",()=>{const source=readFileSync("src/app/api/institutional-assessments/attempts/[attemptId]/submit/route.ts","utf8");expect(source).toContain("status='in_progress' FOR UPDATE");expect(source).toContain("status='evaluated'")});});
